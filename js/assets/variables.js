/**
 * Base URL for all API requests.
 * Used as a prefix for endpoint paths throughout the application.
 * @constant {string}
 */
export const BASE_API_URL = "http://localhost:5678/api/";

/**
 * Path to the homepage file.
 * Used for redirections, particularly after login/logout operations.
 * @constant {string}
 */
export const HOMEPAGE = "index.html";
